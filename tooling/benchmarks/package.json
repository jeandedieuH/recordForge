{
  "name": "@recordforge/editor-benchmarks",
  "private": true,
  "version": "1.4.1",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "benchmark:phase9": "bun run editor-phase9-benchmark.ts",
    "benchmark:cursor-follow": "bun run cursor-follow-path-benchmark.ts",
    "typecheck": "tsc --noEmit",
    "format:check": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@recordforge/contracts": "workspace:*",
    "@recordforge/cursor-core": "workspace:*",
    "@recordforge/domain": "workspace:*",
    "@recordforge/editor-core": "workspace:*",
    "@recordforge/media-core": "workspace:*"
  },
  "devDependencies": {
    "@recordforge/config": "workspace:*",
    "@types/node": "^26.1.2",
    "prettier": "^3.5.6",
    "typescript": "~6.0.3"
  }
}
