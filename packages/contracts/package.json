{
  "name": "@recordforge/contracts",
  "private": true,
  "version": "1.4.6",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint . --report-unused-disable-directives --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "format:check": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@recordforge/config": "workspace:*",
    "@types/node": "^26.4.1",
    "eslint": "^10.8.0",
    "prettier": "^3.5.6",
    "typescript": "~6.0.3",
    "vitest": "^5.0.0"
  }
}
