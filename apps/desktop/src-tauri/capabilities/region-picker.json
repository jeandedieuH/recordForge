{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "region-picker",
  "description": "Minimal permissions for the recordForge fullscreen region selection window",
  "windows": ["region-picker"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-close",
    "core:window:allow-show",
    "core:window:allow-set-focus"
  ]
}
