{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "recording-countdown",
  "description": "Minimal permissions for the recording countdown window",
  "windows": ["countdown"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-close"
  ]
}
